<?php
/**
 * Programador de respaldos automáticos (Fase 2).
 *
 * @package Full_Site_Backup
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Class SSC_Scheduler
 *
 * Stub reservado para la Fase 2. Gestionará los eventos WP-Cron
 * para respaldos diarios/semanales/mensuales.
 */
class SSC_Scheduler {

	/**
	 * Registra los eventos cron del plugin.
	 *
	 * @return void
	 */
	public static function register(): void {
		// Fase 2 — pendiente de implementación.
	}

	/**
	 * Elimina los eventos cron del plugin.
	 *
	 * @return void
	 */
	public static function unregister(): void {
		// Fase 2 — pendiente de implementación.
	}
}
